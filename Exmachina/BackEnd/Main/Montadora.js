class Montadora {
    constructor(cnpj, razaoSocial, marca, contato, telefoneComercial, celular) {
        this.cnpj = cnpj; // CNPJ
        this.razaoSocial = razaoSocial; // Razão social
        this.marca = marca; // Marca
        this.contato = contato; // Contato
        this.telefoneComercial = telefoneComercial; // Telefone comercial
        this.celular = celular; // Telefone celular
    }
}