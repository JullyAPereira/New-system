class Vendedor {
    constructor(codigo, usuario) {
        this.codigo = codigo; // Código
        this.usuario = usuario; // Usuário
        // Demais dados estão no sistema de recursos humanos
    }
}